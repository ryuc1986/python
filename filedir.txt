# -*- coding: utf-8 -*-

#import os
#import shutil

#def show_dir(path):
#    print('=========')
#    for dirpath, dirnames, filenames in os.walk(path):
#        for dirname in dirnames:
#            print(os.path.join(dirpath,dirname))

#tmpdir = '/root/git/tmp'
#show_dir(tmpdir)


#os.mkdir(tmpdir)
#os.makedirs('~/git/tmp/mkdir1/mkdir2')
#show_dir(tmpdir)

#os.rmdir('~/git/tmp/mkdir1/mkdir2')
#show_dir(tmpdir)

#shutil.rmtree(tmpdir)

